// Displays Clerk's built-in sign-in component.
// Users are redirected automatically after successful authentication.
import { SignIn } from '@clerk/nextjs'

export default function Page() {
  return <SignIn />
}
