/**
 * Chart configuration constants
 */

export const CHART_COLORS = [
  'hsl(var(--chart-1))',
  'hsl(var(--chart-2))',
  'hsl(var(--chart-3))',
  'hsl(var(--chart-4))',
  'hsl(var(--chart-5))',
];

/**
 * Default chart styles
 */
export const chartStyles = {
  fontSize: 12,
  tickLine: false,
  axisLine: false,
} as const;
