// Vitest setup file — run before tests to register helpers and matchers
// Import the vitest-specific entry so it uses Vitest's `expect` binding
import '@testing-library/jest-dom/vitest'

// Add any global test setup here (mocks, globals, etc.)